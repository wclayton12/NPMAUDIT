name: build
##test##
on: push
jobs:
  audit:
    runs-on: ubuntu-latest
    name: npm-audit
    steps:
      - uses: actions/checkout@v4
      - name: Audit packages
        run: npm install
      - name: install NPM audit
        run: npm audit -- results.json
